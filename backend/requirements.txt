# FinSight backend dependencies.
# Pinned with ~= (compatible-release): allows patch/minor updates, blocks
# breaking majors. Floors match the versions CI and local dev run against
# (captured 2026-07-14). torch is installed CPU-only from PyTorch's index in
# CI (see .github/workflows/ci.yml) — the version here is the floor, not the source.

fastapi~=0.135.0
uvicorn~=0.42.0
yfinance~=1.2.0
ta~=0.11.0
tavily-python~=0.7.0
transformers~=5.8.0
torch~=2.11.0
accelerate~=0.27
pydantic~=2.12.0
pydantic-settings~=2.13.0
python-dotenv~=1.1.0
httpx~=0.28.0
pytest~=9.0.0
langchain-groq~=0.1
langchain-google-genai~=2.0
PyJWT==2.13.0
sse-starlette~=3.4.0
slowapi~=0.1.10
chromadb~=1.5.0
sentence-transformers~=5.4.0
nsepython~=2.97
